import 'package:flutter/material.dart';

class AppColors {
  static const primary = Color(0xFF1E5F42); // Vert foncé (Button color)
  static const secondary = Color(0xFF2E8F61); // Vert moyen (Highlights)
  static const accent = Color(0xFFA7EAC4); // Vert clair (Button text color)
  static const background = Color(0xFFF5F3E8); // Beige clair (Background)
  static const white = Colors.white;

  // Couleurs supplémentaires pour cohérence
  static const buttonColor = Color(0xFF1E5F42);
  static const buttonTextColor = Color(0xFFA7EAC4);
  static const highlightColor = Color(0xFF2E8F61);
}
